const frutas = ['Maçã', 'banana', 'Pera'];

for (let i in frutas) {
  console.log(frutas[i]);
}

const pessoa = {
  nome: 'Italo',
  sobrenome: 'Canturil',
  idade: 20
}

for (let key in pessoa) {
  console.log(pessoa[key])
}